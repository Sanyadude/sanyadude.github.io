/**
 * LOG_TARGET_TYPE - The targets for the logger
 * @enum {string}
 */
export const LOG_TARGET_TYPE = Object.freeze({
    MEMORY: 'memory',
    CONSOLE: 'console'
});

export default LOG_TARGET_TYPE