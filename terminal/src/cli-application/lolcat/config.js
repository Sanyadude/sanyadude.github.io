export const DEFAULT_SPREAD = 3;
export const DEFAULT_FREQ = 0.1;
export const DEFAULT_SEED = 0;
export const DEFAULT_INVERT_MODE = false;

export const CONFIG = Object.freeze({
    DEFAULT_SPREAD,
    DEFAULT_FREQ,
    DEFAULT_SEED,
    DEFAULT_INVERT_MODE,
});

export default CONFIG